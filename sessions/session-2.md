# Сессия 2 — 24 августа 2026

**Цель сессии.** Написать спецификацию стенда рекуррентных платежей: сначала собрать все продуктовые решения в отдельный файл ответов, потом собрать SPEC.md только из него, без кода.

**Что сделано.**

- Агент прочитал AGENTS.md и задал 46 вопросов по местам, где иначе решал бы сам: состояния и переходы, инициатор списания, нехватка allowance и баланса, определение активной подписки, повторные и преждевременные списания, отмена и повторная подписка, границы времени.
- Ответы записаны дословно в SPEC-ANSWERS.md. По ходу проверки документа файл дорос до 59 ответов: 47 — порядок проверок внутри вызова списания, 48 — состояния витрины, 49 — единая величина debtPeriods, 50-54 и 55-59 — закрытие пропусков, найденных при написании спецификации.
- Продукт назван: "Минута", подписной фид фактов о платежах и блокчейне.
- Написан SPEC.md, 16 разделов: роли, модель платежа, хранение, состояния подписки, правила списания, время, оформление и отмена, события, витрина, развертывание, контрольные значения, обязательные сценарии, допущения, нецели. Каждое правило со ссылкой на номер ответа.
- Контрольные значения К1-К11 посчитаны из правил документа до кода: суммы в базовых единицах, границы предела просрочки в секундах (240-299 -> debtPeriods 5, от 300 -> 6), сценарий погашения долга по шагам, точка исчерпания разрешения раньше баланса.
- Раздел "Не определено" по ходу сессии прошел путь 11 пунктов -> 5 -> пусто.
- Создан REPORT-NOTES.md — черновик заметок к итоговому отчету.
- Кода не написано ни строки, Solidity в документах нет.

**Что не получилось.**

- Одно сообщение с правками пришло с обрывом на середине фразы. Агент внес независимую часть правки, а оборванную не тронул и попросил окончание, вместо того чтобы дописать фразу за меня. Потеря — один круг переписки.

**Размышления.**

- Работа шла в одну сторону: сначала 59 ответов на вопросы агента,
  потом спецификация только из этого файла со ссылками на номера ответов.
  Ни одно правило в SPEC.md не появилось из головы агента.
- Агент за сессию поймал пять противоречий в моих же ответах и ни одно
  не решил сам: расхождение в наборе полей, неопределенную границу
  предела просрочки, незаданный порядок проверок внутри вызова списания,
  конфликт обнуления счетчика неудач с требованием показывать причину
  последней неудачи, одну величину под двумя именами. Четвертое вскрыло
  целое состояние витрины, которого в спецификации не было: платеж прошел,
  доступа еще нет, ничего не сломано.
- Одно расхождение оказалось моей ошибкой в постановке, а не неточностью
  формулировки: порядок экранов витрины, который я задал, перекрывал
  правило доступа. Правило было верным, порядок неверным. Правил порядок.
- Одиннадцать пропусков агент вынес списком вместо того, чтобы заполнить
  их правдоподобными значениями. Половина из них блокировала тесты —
  имена событий, начальные значения полей, длина периода.
- Контрольные значения посчитаны до написания кода и код под них
  подгоняться не будет. Это перенесено из первого задания, где сработало.

**Время.** не зафиксировано.
