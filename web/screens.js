// screens.js — тексты витрины стенда «Минута».
//
// Модуль содержит только тексты и структуру пяти экранов раздела 10 SPEC.md.
// Логики здесь нет: ни обращений к узлу, ни к DOM, ни вычисления состояния.
// Какой экран показывать, решает web/app.js по правилам раздела 10;
// этот модуль лишь отдает готовые заголовки, пояснения и подписи кнопок.
//
// Экспортируется:
//
//   SCREEN_IDS          — идентификаторы пяти экранов (объект-перечисление).
//   SCREENS             — тексты экранов, ключи из SCREEN_IDS.
//   FAILURE_REASONS     — подсказки по lastFailureReason, ключ — число 0/1/2.
//   ACTION_LABELS       — подписи кнопок действий.
//   ROLE_LABELS         — названия ролей предустановленных аккаунтов.
//   ACTION_NOTES        — пояснения к недоступным действиям.
//   TOKEN_BLOCK         — блок токена подписчика (о. 63).
//   REVERT_REASONS      — откаты контракта человеческим текстом.
//
// Форма экрана (SCREENS[id]):
//
//   id           — строка, совпадает со значением из SCREEN_IDS;
//   title        — заголовок экрана;
//   text         — поясняющий текст: строка либо функция-шаблон,
//                  принимающая число и возвращающая строку;
//   actionLabel  — подпись кнопки действия либо null, если кнопки нет.
//
// Отдельные экраны добавляют свои поля — они описаны рядом с экраном.
// Все функции-шаблоны чистые: принимают число или строку, возвращают строку.

/** Идентификаторы экранов витрины. */
export const SCREEN_IDS = {
  NO_SUBSCRIPTION: 'noSubscription',
  ACCESS_OPEN: 'accessOpen',
  CANCELED: 'canceled',
  DEBT_CATCHUP: 'debtCatchup',
  OVERDUE: 'overdue',
};

/** Подписи кнопок действий витрины. */
export const ACTION_LABELS = {
  subscribe: 'Оформить подписку',
  charge: 'Списать за период',
  retryCharge: 'Повторить списание',
  cancel: 'Отменить подписку',
};

/** Названия ролей трех предустановленных аккаунтов стенда. */
export const ROLE_LABELS = {
  subscriber: 'Подписчик',
  recipient: 'Получатель',
  outsider: 'Посторонний',
};

// Склонение слова «период» по числу: 1 период, 2 периода, 5 периодов.
function periodsWord(n) {
  const abs = Math.abs(n);
  const tail100 = abs % 100;
  const tail10 = abs % 10;
  if (tail100 >= 11 && tail100 <= 14) return 'периодов';
  if (tail10 === 1) return 'период';
  if (tail10 >= 2 && tail10 <= 4) return 'периода';
  return 'периодов';
}

// Склонение слова «попытка» по числу: 1 попытка, 2 попытки, 5 попыток.
function attemptsWord(n) {
  const abs = Math.abs(n);
  const tail100 = abs % 100;
  const tail10 = abs % 10;
  if (tail100 >= 11 && tail100 <= 14) return 'попыток';
  if (tail10 === 1) return 'попытка';
  if (tail10 >= 2 && tail10 <= 4) return 'попытки';
  return 'попыток';
}

/**
 * Подсказки по причине последней неудачи.
 * Ключ — числовое значение lastFailureReason из контракта (uint8).
 * Своего перечисления фронт не заводит: читает число напрямую.
 */
export const FAILURE_REASONS = {
  0: {
    code: 0,
    reason: 'Причины нет',
    hint: 'Неудачных попыток списания не было.',
  },
  1: {
    code: 1,
    reason: 'Не хватает разрешения',
    hint: 'Поднимите разрешение на списание: контракту подписки нужен предел не меньше суммы периода.',
  },
  2: {
    code: 2,
    reason: 'Не хватает баланса',
    hint: 'Пополните баланс подписчика: на счете меньше суммы периода.',
  },
};

/** Тексты пяти экранов витрины. */
export const SCREENS = {
  // Экран 1: записи о подписке нет.
  [SCREEN_IDS.NO_SUBSCRIPTION]: {
    id: SCREEN_IDS.NO_SUBSCRIPTION,
    title: 'Нет подписки',
    text: 'Подписки по этому адресу нет. Оформите подписку, чтобы видеть факт минуты: оформление и первое списание — один вызов.',
    actionLabel: ACTION_LABELS.subscribe,
  },

  // Экран 2: оплаченный период еще не кончился, показывается факт минуты.
  // Кнопки списания на этом экране нет.
  // Дополнительное поле:
  //   canceledNotice(expiresAt) — текст плашки для отмененной подписки
  //                               с неистекшим оплаченным периодом;
  //                               expiresAt — уже отформатированное время
  //                               истечения (строка).
  [SCREEN_IDS.ACCESS_OPEN]: {
    id: SCREEN_IDS.ACCESS_OPEN,
    title: 'Доступ открыт',
    text: 'Период оплачен, фид доступен. Факт минуты меняется с каждым успешным списанием.',
    actionLabel: null,
    canceledNotice: (expiresAt) =>
      `Подписка отменена, доступ сохраняется до конца оплаченного периода — до ${expiresAt}.`,
  },

  // Экран 3: подписка отменена, оплаченный период истек.
  // Долг не показывается: он сгорел вместе с отменой.
  [SCREEN_IDS.CANCELED]: {
    id: SCREEN_IDS.CANCELED,
    title: 'Подписка отменена',
    text: 'Оплаченный период кончился, списаний больше не будет. Отмена терминальна: вернуться можно только новой подпиской.',
    actionLabel: ACTION_LABELS.subscribe,
  },

  // Экран 4: неудачных попыток не было, но оплаченный период в прошлом.
  // Причина неудачи не показывается — ее нет.
  // Поля:
  //   text(n)  — «осталось догнать N периодов», N = debtPeriods;
  //   hint     — пояснение про один период за нажатие.
  [SCREEN_IDS.DEBT_CATCHUP]: {
    id: SCREEN_IDS.DEBT_CATCHUP,
    title: 'Погашение долга',
    text: (n) => `Осталось догнать ${n} ${periodsWord(n)}.`,
    hint: 'Каждое нажатие закрывает ровно один период. Пока долг больше одного периода, деньги уходят, а доступ еще не открывается.',
    actionLabel: ACTION_LABELS.charge,
  },

  // Экран 5: была неудачная попытка списания.
  // Поля:
  //   text(n)       — «просрочено N периодов», N = debtPeriods;
  //   attemptText(k)— номер попытки, k = failedAttempts;
  //   reasonLabel   — подпись перед причиной неудачи;
  //   hintLabel     — подпись перед подсказкой.
  // Причину и подсказку берите из FAILURE_REASONS по числу lastFailureReason.
  [SCREEN_IDS.OVERDUE]: {
    id: SCREEN_IDS.OVERDUE,
    title: 'Подписка просрочена',
    text: (n) => `Просрочено ${n} ${periodsWord(n)}.`,
    attemptText: (k) => `Подряд неудачных попыток: ${k} ${attemptsWord(k)}.`,
    reasonLabel: 'Причина последней неудачи',
    hintLabel: 'Что сделать',
    actionLabel: ACTION_LABELS.retryCharge,
  },
};

/**
 * Пояснения к действиям, недоступным выбранному аккаунту.
 *
 * Стенд выдает баланс и разрешение только подписчику (раздел 11), а отменять
 * подписку по правилу о. 36 может лишь он сам. Списание при этом доступно
 * всем трем аккаунтам — и ломать это нельзя: то, что списание может вызвать
 * кто угодно, и есть свойство pull-модели, ради показа которого посторонний
 * адрес вообще существует.
 */
export const ACTION_NOTES = {
  otherAccount:
    'Баланс и разрешение стенд выдает только подписчику, поэтому оформить подписку можно от него; отменяет подписку тоже только он сам. Списание при этом доступно всем трем аккаунтам: вызвать его может кто угодно, и ради этого посторонний адрес здесь и нужен.',
};

/**
 * Откаты контракта человеческим текстом.
 *
 * Ключи проставляет web/app.js, разбирая ответ узла: строку из `require`
 * либо селектор ошибки токена. Технической строки вида
 * «allowance below period price» на витрине быть не должно — она ничего
 * не объясняет тому, кто смотрит на стенд впервые.
 *
 * `unknown` — запасной случай: причина показывается как есть, но помечается
 * необработанной, чтобы ее было видно и не приняли за нормальный текст.
 */
export const REVERT_REASONS = {
  /// Оформление при нехватке разрешения (раздел 8, о. 29).
  allowanceTooLow:
    'Разрешения не хватает даже на один период. Подписчик должен разрешить контракту списывать со своего счета сумму периода — без этого оформление не проходит.',

  /// Перевод не прошел: на счете меньше суммы периода.
  balanceTooLow: 'Баланса не хватает: на счете меньше суммы одного периода.',

  /// Списание раньше срока (раздел 6, путь 3).
  notDueYet:
    'Срок еще не наступил: текущий период оплачен. Предоплата вперед не предусмотрена — списание станет возможным в секунду, когда оплаченный период кончится.',

  /// Оформление при уже активной подписке (раздел 8).
  alreadySubscribed:
    'Подписка по этому адресу уже есть. Оформить вторую поверх активной нельзя — сначала отмена.',

  /// Списание или отмена по адресу без записи (раздел 6, путь 1).
  noSubscription: 'Подписки по этому адресу нет: оформлять и отменять нечего.',

  /// Списание по отмененной подписке (путь 2) и повторная отмена.
  subscriptionCanceled:
    'Подписка отменена. Списаний по ней больше не будет, и отменить ее второй раз нельзя: отмена терминальна, вернуться можно только новой подпиской.',

  /// Причина, которой нет в списке выше.
  unknown: (raw) => `Необработанный случай, контракт ответил так: ${raw}`,
};

/**
 * Блок токена подписчика (о. 63, раздел 10 спецификации).
 *
 * Это единственное отступление от правила «ничего сверх спецификации»,
 * и оформлено оно отдельным блоком, а не четвертой кнопкой в ряду действий
 * подписки: `approve` — действие пользователя в его собственном токене,
 * а не функция контракта подписки, и интерфейс обязан показывать разницу.
 *
 * Баланс и разрешение стоят рядом намеренно: расхождение этих двух величин
 * и есть содержание сценария о. 57 — разрешения хватает на 5 периодов,
 * баланса на 10, поэтому разрешение кончается первым.
 */
export const TOKEN_BLOCK = {
  title: 'Токен подписчика',

  balanceLabel: 'Баланс',
  allowanceLabel: 'Разрешено списывать',

  /// Подпись кнопки. Величина фиксированная: ввода суммы нет, он остается
  /// нецелью — это уже интерфейс кошелька (раздел 15).
  actionLabel: 'Разрешить еще 5 периодов',

  /// Зачем блок нужен. Читается под кнопкой, объясняет механику, а не
  /// пересказывает надпись на ней.
  text: 'Разрешение — единственный рычаг подписчика: контракт списывает не больше того, что ему разрешили. Стенд выдает разрешение на 5 периодов, а баланс на 10, поэтому разрешение кончится первым — на шестом периоде списание не пройдет с причиной «не хватает разрешения».',

  /// Строка для чужого аккаунта: разрешение выдает владелец счета.
  otherAccount: 'Разрешение выдает владелец счета, поэтому поднять его можно только от подписчика. Показаны баланс и разрешение подписчика — та подписка, состояние которой на экране.',

  /// Сумма в базовых единицах в читаемый вид: 6 знаков после запятой.
  amount: (base, symbol) => {
    const units = BigInt(base);
    const whole = units / 1000000n;
    const frac = (units % 1000000n).toString().padStart(6, '0').replace(/0+$/, '');
    return `${whole}${frac ? ',' + frac : ''} ${symbol}`;
  },

  /// Сколько это периодов — величина, в которой стенд и мыслит.
  periods: (base, price) => {
    const n = Number(BigInt(base) / BigInt(price));
    return `${n} ${n % 10 === 1 && n % 100 !== 11 ? 'период' : n % 10 >= 2 && n % 10 <= 4 && (n % 100 < 11 || n % 100 > 14) ? 'периода' : 'периодов'}`;
  },
};
